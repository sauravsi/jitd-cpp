
Use this command to complile single file version:

g++ jitd.cpp -o jitd

OR

Use this command to complile single multi-file version:

g++ -I include/ -I src/ src/*.cpp -o jitd

then execute:

./jitd
